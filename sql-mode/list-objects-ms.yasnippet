# -*- mode: snippet; coding: utf-8 -*-
# contributor: Pablo Mercader Alcántara <programingfrik@gmail.com>
# name: list-objects-ms
# key: liobms
# expand-env: ((yas/indent-line 'fixed))
# --
select
    (sc.name + '.' + ob.name) objeto,
    ob.type
from sys.objects ob
inner join  sys.schemas sc on ob.schema_id = sc.schema_id
where (ob.type in ('U', 'V', 'P'))
order by ob.type, sc.name, ob.name
go$0
